.wrapper
  .header
    .header__left
      .header__left-home
        VGyoU
      .header__left-search
        Search
    .header__right
      .header__right__sign
        - if user_signed_in? 
          = link_to "マイページ",  "/users/#{current_user.id}"
        - else
          = link_to "新規会員登録","/users/sign_up", class: "up"
          = link_to "ログイン","/users/sign_in", class: "in"
  .list